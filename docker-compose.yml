version: '3.8'

services:
  client:
    build: .
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DOCKER_HOST=unix:///var/run/docker.sock
    env_file:
      - .env
    stdin_open: true
    tty: true