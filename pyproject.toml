[project]
name = "open-weather-mcp-client"
version = "0.1.0"
description = "Client application using OpenWeather MCP server via Docker"
requires-python = ">=3.11,<3.14"
dependencies = [
    "mcp",
    "python-dotenv",
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "psycopg2-binary",
    "alembic",
]